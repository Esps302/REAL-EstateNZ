const admin = require('firebase-admin');
const fs = require('fs');

// Initialize Firebase Admin (assuming default app is initialized or we can use local emulator, wait we don't have the service account key here)
